{
    "configurations": [
        {
            "name": "ROS: Launch",
            "type": "ros",
            "request": "launch",
            "target": "${workspaceFolder}/install/auto_aim/share/auto_aim/launch/auto_aim_node_launch.py"
        },

    {
        "name": "(gdb) Launch",
        "type": "cppdbg",
        "request": "launch",
        "cwd": "${workspaceFolder}",
        "program": "${workspaceFolder}/install/auto_aim/lib/auto_aim/auto_aim_node",
        "MIMode": "gdb",
    }
    ]
}